import ParameterKendaraanSection from '../ParameterKendaraanSection';

export default function ParameterKendaraanForm({ onBack }) {
  return (
    <ParameterKendaraanSection onBack={onBack} />
  );
}
